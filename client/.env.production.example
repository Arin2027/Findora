# Findora client — production environment template
# Set in Vercel → Project → Settings → Environment Variables
# Apply to Production (and Preview if you use a stable preview API URL)

# Required: your Render API URL (HTTPS, no trailing slash)
VITE_API_URL=https://findora-api.onrender.com
